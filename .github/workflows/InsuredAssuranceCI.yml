name: InsuredAssurance CI Pipeline

on: 
  push:
    branches: 
      - '*'
  pull_request: 
    branches: 
      - '*'
  workflow_dispatch: 
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3 # Action to clone the repository to the runner

      
